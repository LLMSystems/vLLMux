[pytest]
testpaths = tests
markers =
    unit: pure-logic unit tests (no IO)
    api: FastAPI TestClient route/contract tests
    integration: requires a live server or GPU (skipped by default)
asyncio_mode = auto
