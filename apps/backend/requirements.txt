uvicorn
fastapi
pydantic
pyyaml
httpx
psutil
aiosqlite
# Postgres backend for the shared store (HA mode, LLMOPS_DB_URL=postgres://…).
# Only imported when configured; SQLite remains the zero-config default.
asyncpg
huggingface_hub
# SSO/OIDC: sign+verify session cookies (HS256) and validate IdP id_tokens
# (RS256 via JWKS). [crypto] pulls `cryptography` for RSA signature verification.
pyjwt[crypto]
# [perf] = load-test deps (aiohttp/jinja2/numpy/transformers…); [ifeval] = langdetect+nltk
# for the IFEval accuracy benchmark. Targeted extras only — NOT [all] (that pulls
# multi-GB opencompass/vlmeval backends we don't use).
evalscope[perf,ifeval]
# On-the-fly weight quantization for the vLLM subprocess: `--quantization
# bitsandbytes` loads any unquantized model in 4-bit (NF4) / 8-bit. 0.49.2+ builds
# support the CUDA 13 base image; verified working on Ampere (RTX 3060 Ti).
bitsandbytes>=0.49.2